# 开发日志

## v2.1 — 2026-05-19

### 新增

- **网速监测**：主机状态页新增实时上传/下载速率显示，通过 `netstat` 采样主网口字节计数计算，自适应 B/s → KB/s → MB/s
- **Ollama 检测**：新增 Ollama 本地大模型平台检测，检查 CLI 安装状态及后台服务是否运行

### 变更

- **移除 Chrome 和 macOS 卡片**：这两项不属于开发环境配置范畴，精简至 24 项
- **检测项总数**：25 → 24

### 修复

- **磁盘使用率虚低**：APFS 的 `df -g` Used 列仅统计当前卷独占数据，改为 `Total - Available` 计算反映容器内所有卷和快照的真实占用
- **网速始终为 0**：`hasPrefix("en1")` 误匹配 `en10` 等不活跃接口导致提前返回，改为精确前缀匹配（接口名后必须跟空格/Tab）；同时适配有/无 MAC 地址两种列数格式

---

## v2.0 — 2026-05-18

### 新增

- **主机状态监测**：新增"主机状态"标签页，实时显示 CPU 使用率、内存占用、磁盘用量、系统运行时间，进度条颜色根据负载自动变化（绿→橙→红）
- **标签页切换**：顶部新增分段选择器，在"开发环境"和"主机状态"之间切换

### 优化

- **卡片式网格布局**：环境项从纵向列表改为 3 列卡片式网格（LazyVGrid），更紧凑美观，一屏可见更多信息
- **状态图标位置**：✅/❌/⚠️ 状态图标移至名称右侧紧贴显示，一目了然
- **交互模式调整**：点击卡片 → 底部展开详情面板（显示路径、版本、状态标签）；点击详情面板中的名称 → 打开官网。不再直接跳转浏览器
- **详情面板**：底部固定显示栏，带关闭按钮，可查看安装路径、版本号、运行状态
- **视觉微调**：卡片支持选中高亮（蓝色边框 + 浅蓝底色），窗口尺寸适配 3 列布局

### 数据模型

- 新增 `HostStatus` 结构体，包含 CPU/内存/磁盘/运行时间字段
- `EnvChecker` 新增 `hostStatus` 属性及 `collectHostStatus()` 采集方法
- 主机状态随自动刷新同步更新

---

## v1.1 — 2026-05-17

- 检测项从 10 项扩展至 25 项
- 新增 DMG 安装包及安装说明

---

## v1.0 — 2026-05-16

- 首次发布
- 检测 10 项开发环境（Homebrew、Node.js、Git 等）
- 半透明毛玻璃窗口，支持置顶
- 手动刷新 + 定时自动刷新
- 点击工具行打开官网
